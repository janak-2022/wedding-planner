import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors
  static const primary = Color(0xFFFF5A79);
  static const secondary = Color(0xFFB095DD);
  static const tertiary = Color(0xFFFFF5EA);
  
  // Background colors
  static const background = Color(0xFFF8F8F8);
  static const surface = Colors.white;
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
  
  // Text colors
  static const textDark = Color(0xFF1A1A1A);
  static const textMedium = Color(0xFF666666);
  static const textLight = Color(0xFFEEEEEE);
  
  // Semantic colors
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFC107);
  static const error = Color(0xFFE53935);
  static const info = Color(0xFF2196F3);
  
  // Gradient colors
  static const gradientStart = Color(0xFFFF5A79);
  static const gradientEnd = Color(0xFFFF8C9E);
  
  // Transparent
  static const transparent = Colors.transparent;
  
  // Overlay
  static const overlay = Color(0x80000000);
  
  // Shades of primary color
  static const primaryLight = Color(0xFFFFB3C0);
  static const primaryDark = Color(0xFFCC4861);
  
  // Shades of secondary color
  static const secondaryLight = Color(0xFFD7C9ED);
  static const secondaryDark = Color(0xFF8D77B0);
}